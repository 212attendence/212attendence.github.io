name: Remove legacy admin login
on:
  push:
    paths:
      - 'scripts/remove-legacy-admin-login.py'
      - '.github/workflows/remove-legacy-admin-login.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/remove-legacy-admin-login.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/remove-legacy-admin-login.yml scripts/remove-legacy-admin-login.py
          git add -A
          git commit -m "Remove URL-based administrator password login" || exit 0
          git push
